import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.triumph.whitesands',
  appName: 'Triumph White Sands',
  webDir: 'dist'
};

export default config;
